<% import com.example.app._ %>
<%@ val usuarios: List[((Usuario, Int), Int)] = Nil %>
<%@ val mensajes: List[((MensajeChat,Usuario), Int)] %>
<%@ val user: Usuario = null%>

<table class="table">
    <thead>
      <tr>
         <th>Posici&oacute;n</th>
         <th>Kangri</th>
         <th>Puntuaci&oacute;n</th>
      </tr>
     </thead>
     <tbody>
      #for (((u,pts),pos) <- usuarios)
      <tr>
          <td><a href="/predicciones/${u.ident}">${(pos + 1)}</a></td>
          <td><a href="/predicciones/${u.ident}">${u.ident}</a></td>
          <td><a href="/predicciones/${u.ident}">${pts}</a></td>
      </tr>
      #end
     </tbody>
</table>

